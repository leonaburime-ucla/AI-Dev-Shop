# Project Brief - Topology Tradeoffs Fixture

The user needs macro options before a blueprint is finalized. The system has a web UI, API backend, background worker, relational data, and strict constraints against external SaaS in one variant.
